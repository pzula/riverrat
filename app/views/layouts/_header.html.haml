%header
  %p Welcome to River Rat
- if current_user
  %p Logged in as
  = current_user.email
  =  link_to "Logout", session_path("current"), :method => :delete
- else
  = link_to "Sign Up", new_user_path, :method => :get
  = link_to "Login", new_session_path, :method => :get
