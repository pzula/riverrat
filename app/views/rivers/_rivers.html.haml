%li
  = link_to "#{rivers.name}", river_path(rivers.id)
  %span{:class => 'river-favorite'}
    - if current_user
      = link_to image_tag('river-rat-star.png'), river_favorites_path(rivers.id), method: :post
